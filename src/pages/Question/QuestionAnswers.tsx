import React from 'react'
import { ReplyType, Replies } from '../../typings/typings'

const QuestionAnswers: React.FC<Replies> = ({ replies }) => {
  return <div className="py-8 m-auto max-w-4xl p-4"></div>
}

export default QuestionAnswers
